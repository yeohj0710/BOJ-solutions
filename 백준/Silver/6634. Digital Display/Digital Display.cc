#include <bits/stdc++.h>
#define int long long
using namespace std;

main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL), cout.tie(NULL);

    string num[7][11] = {{"+---+", "    +", "+---+", "+---+", "+   +", "+---+", "+---+", "+---+", "+---+", "+---+", " "},
                          {"|   |", "    |", "    |", "    |", "|   |", "|    ", "|    ", "    |", "|   |", "|   |", " "},
                          {"|   |", "    |", "    |", "    |", "|   |", "|    ", "|    ", "    |", "|   |", "|   |", "o"},
                          {"+   +", "    +", "+---+", "+---+", "+---+", "+---+", "+---+", "    +", "+---+", "+---+", " "},
                          {"|   |", "    |", "|    ", "    |", "    |", "    |", "|   |", "    |", "|   |", "    |", "o"},
                          {"|   |", "    |", "|    ", "    |", "    |", "    |", "|   |", "    |", "|   |", "    |", " "},
                          {"+---+", "    +", "+---+", "+---+", "    +", "+---+", "+---+", "    +", "+---+", "+---+", " "}};


    while(true) {
        string str; cin >> str;
        if(str == "end") break;

        for(int i=0; i<7; i++) {
            for(int j=0; j<5; j++) {
                if(j == 2) {
                    cout << num[i][10] << "  ";
                    continue;
                }

                cout << num[i][str[j] - '0'];
                if(j != 4) cout << "  ";
            }
            cout << "\n";
        }

        cout << "\n\n";
    }

    cout << "end\n";
}
