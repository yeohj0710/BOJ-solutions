#include <bits/stdc++.h>
#define int long long
using namespace std;

signed main() {
    ios::sync_with_stdio(0), cin.tie(0);

    while(true) {
        int N; cin >> N;
        if(N == 0) break;

        cout << (N + 1) / 2 << "\n";
    }
}
