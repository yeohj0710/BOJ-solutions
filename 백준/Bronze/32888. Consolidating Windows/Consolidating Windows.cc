#include <bits/stdc++.h>
#define int long long
using namespace std;

int32_t main() {
    cin.tie(0)->sync_with_stdio(0);

    double a, b; cin >> a >> b;

    cout << fixed << setprecision(8);
    cout << sqrt(pow(a, 2) + pow(b, 2)) << "\n";
}
