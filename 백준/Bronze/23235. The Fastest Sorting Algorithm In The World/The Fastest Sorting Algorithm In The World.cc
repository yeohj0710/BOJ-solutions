#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL), cout.tie(NULL);

    int t = 1;

    while(true) {
        int N; cin >> N;
        if(N == 0) break;

        while(N--) {
            int x; cin >> x;
        }

        cout << "Case " << t << ": Sorting... done!\n";

        t++;
    }
}
