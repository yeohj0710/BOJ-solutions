#include <bits/stdc++.h>
#define int long long
using namespace std;

int32_t main() {
    cin.tie(0)->sync_with_stdio(0);

    int n; cin >> n;

    vector<int> v = {2024, 2025, 2025, 2026, 2026};
    vector<int> u = {8, 3, 10, 5, 12};

    cout << v[n-1] << " " << u[n-1] << "\n";
}
