#include <bits/stdc++.h>
#define int long long
using namespace std;

signed main() {
    ios::sync_with_stdio(0), cin.tie(0);

    int a, b, c; char x, y;
    cin >> a >> x >> b >> y >> c;

    if(a + b == c) cout << "YES\n";
    else cout << "NO\n";
}
