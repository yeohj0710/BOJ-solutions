#include <bits/stdc++.h>
using namespace std;

int main() {
    int T, n;
    char c;
    cin >> T;
    while(T--) {
        cin >> n >> c;
        while(n--) cout << c;
        cout << "\n";
    }
}
