#include <bits/stdc++.h>
#define int long long
using namespace std;

int32_t main() {
    cin.tie(0)->sync_with_stdio(0);

    double x; cin >> x;

    cout << "YES\n";

    cout << (int)(x * 1e9) << " " << (int)(1e9) << "\n";
}
