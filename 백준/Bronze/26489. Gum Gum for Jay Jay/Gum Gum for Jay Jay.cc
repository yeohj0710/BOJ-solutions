#include <bits/stdc++.h>
#define int long long
using namespace std;

signed main() {
    ios::sync_with_stdio(0), cin.tie(0);

    string str;
    int ans = 0;

    while(getline(cin, str)) ans++;

    cout << ans << "\n";
}
